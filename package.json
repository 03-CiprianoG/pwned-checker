{
  "name": "pwnedchecker",
  "version": "1.0.0",
  "type": "module",
  "description": "Software to check if a password is already been leaked using the pwned API ",
  "main": "pwned.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Giuseppe Cipriano",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "node-fetch": "^3.0.0",
    "prompt": "^1.2.0"
  }
}
